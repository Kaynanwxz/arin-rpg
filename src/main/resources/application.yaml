spring:
  application:
    name: arin-rpg

  datasource:
    url: jdbc:h2:file:./data/arin-rpg
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true